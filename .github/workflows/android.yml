name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # إيقاف كاش Gradle
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'
          cache-disabled: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK & CMake
        run: |
          sdkmanager --install "ndk;26.1.10909125" "cmake;3.22.1"

      # تحميل مكتبة GLM
      - name: Download GLM Library
        run: |
          if [ ! -d "lib/glm" ]; then
            git clone --depth 1 https://github.com/g-truc/glm.git lib/glm
          fi

      # حل عبقري: إنشاء جسر glad للأندرويد تلقائياً لمنع أي خطأ missing glad
      - name: Setup OpenGL Header Bridge
        run: |
          mkdir -p include/glad
          cat << 'EOF' > include/glad/glad.h
          #pragma once
          #ifdef __ANDROID__
              #include <GLES3/gl3.h>
              #include <EGL/egl.h>
          #endif
          EOF

      # تشغيل البناء
      - name: Build Android APK with Gradle
        run: |
          cd android
          gradle assembleDebug --no-daemon --console=plain

      # رفع ملف الـ APK النهائي
      - name: Upload APK as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MinecraftClone-Android-APK
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 7
